---
name: loewe-editorial-craft
description: 手作拼贴编辑风的横向翻页网页 PPT 生成系统，源自 LOEWE 180 周年刊物视觉。红蓝双电压（朱砂红 + anagram 蓝）、暖纸画布、深茄紫负空间、撕边/吊牌/色纸块/静物组/手写批注等拼贴母语。当用户要求用 LOEWE 风格、拼贴编辑风、红蓝撞色编辑感、杂志手作风做方案/PPT/deck，或说「用罗意威那套」「用 loewe 那个 skill 做个方案」时使用。产出单 HTML 横向翻页 deck（键盘翻页、目录跳页、卡片点开看、点空白还原）。
---

# LOEWE Editorial Craft · 手作拼贴编辑风 Deck 生成

## 必读顺序

1. 先读 `references/DESIGN.md`——完整设计规范（色票、字体、组件、Do/Don't 硬禁令），所有生成必须遵守
2. 再读 `references/text-discipline.md`——文字收束十条（kicker 制度、字数分档、最小字号、三声部纪律等），写文案时遵守
3. 以 `assets/sample-deck.html` 为基准——12 页完整样张，复用其骨架（head 字体引入、CSS token、翻页/跳页/放大三件交互脚本），按用户内容替换页面
4. 需要回答「这套规范为什么这么定」时读 `references/origin-story.md`——原始素材拆解记录（素材 → 结构决策的映射），迭代规范时先读它再改

## 生成工作流

1. **先定叙事弧**：按 12 页型组织内容——封面 / 目录 / 章节页 / 正文图文 / 清单页 / 巨数页 / 图表页 / 对比页 / 时间轴 / 金句页 / 封底。内容少的可合并页型，但每页型在整套里只能用一种构图
2. **拷贝基准**：以 sample-deck.html 的 `<style>` token 和 `<script>` 交互为骨架，新建 index.html
3. **逐页构图**：每页一个不同构图，全部用拼贴母语（撕边纸签、实底吊牌、色纸块骑缝、静物方块、大号衬线序号、手写批注），每页至少三层视觉咬合（字层 + 色块/静物层 + 批注/标签层）
4. **文字收束**：先写文案再排版；kicker 每页独一份判断句 ≤18 字；标题超档先改文案再降字号
5. **自检后交付**：逐页截图核对 DESIGN.md 的 Don't 清单，确认无违禁成分后交付

## 不可碰的地基（违反即返工）

- **禁止细线装饰成分**：卡片描边、骑细彩条、行分隔线、中缝虚线、虚线网格、双细规线——分隔交给留白、色块、字级差
- **禁止同质卡片复读**：「mono 小标 + 大字 + 一行说明」同骨架排排坐是 AI 舒适区；清单用编号式编辑排版，对比用明暗双版尺度差
- **禁止内容载具旋转**：色版、卡片、吊牌、纸签、静物一律 rotate(0)。材质感来自纸/绒/漆/撕边/错位实色影，不来自倾斜。仅手写批注与小兽 ≤3° 例外
- 禁止纯白画布纯黑文字、饼图、圆角卡片、玻璃拟态、阴影层级系统、蓝色手写体

## 可探索的延伸

在遵守上面地基的前提下，鼓励按内容发明新构图：页型的拼贴组合方式、静物组的选材与摆位、手写批注的措辞与走向、红蓝双电压的配比——这些是每套方案该长出的新东西，不要照抄样张的页面布局。

## 交互三件套（必须保留）

- ← → 翻页（回弹曲线 `cubic-bezier(.32,.72,0,1)`）
- 目录行点击跳页（`data-goto`）
- `.zoomable` 卡片点开克隆放大、点任意处还原（`data-extra` 承载展开详情）
